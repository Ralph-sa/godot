# M15 编辑器核心工作流 Spec（基于代码核对 2026-08-17）

## 代码事实
- 启动路径：--editor --path 默认项目**直进编辑器**（项目管理器被跳过——
  main_ohos.cpp 首次启动自动创建 default_project）
- **项目管理器（ProjectManager）不可达**：创建项目向导、项目列表、最近项目、
  导入项目（zip）全部被跳过——**这是功能缺失，不是设计选择**

## 决策（2026-08-17 用户拍板）
- **维持默认项目**：启动直进 default_project 编辑器，不恢复项目管理器。
  项目创建/导入入口列为已知限制（后续若需要再议）。

## 任务
- [ ] T-EW-1 文档标注：TESTING.md 与 legacy 清单记录「直进默认项目（决策），
      项目管理器入口不可达（已知限制）」

## 开放项
- 未来是否需要项目切换/新建入口（在编辑器内实现项目列表 vs 恢复 PM）——暂缓。