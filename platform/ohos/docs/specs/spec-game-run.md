# M18 游戏运行 Spec（基于代码核对 2026-08-17）

## 代码事实
- 编辑器运行机制（editor_run/editor_run_bar）编入 ✅
- 运行路径：OS_OHOS::create_instance 进程内重启（main_ohos 已实现 restart_args 传递——
  --path 项目 + 场景参数）
- **运行游戏的实际链路未验证**：编辑器点「播放」→ 进程内重启 → 场景运行，
  从未实测过任何一步

## 验收标准
- ① 编辑器「播放」触发 create_instance 重启（诊断日志 restart 记录）
- ② 重启后引擎以游戏模式运行场景（MainLoop 切换）
- ③ 停止播放回到编辑器（再次重启或状态恢复）

## 任务
- [ ] T-GR-1 模拟器验证播放链路：注入点击播放按钮 → restart 日志 → 运行状态
- [ ] T-GR-2 停止播放链路验证

## 开放项
- 模拟器画面不刷新——运行画面不可见，只能验证状态机与日志。