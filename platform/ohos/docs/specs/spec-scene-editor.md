# M16 场景编辑 Spec（基于代码核对 2026-08-17）

## 代码事实
- 编辑器 UI（视口/节点树/检查器/菜单）已上屏 ✅（T03 实证）
- 输入链已通 ✅（T04/T07 实证——引擎处理点击）
- **模拟器画面不刷新**：编辑操作的画面级反馈无法在模拟器验证——
  只能验证到"引擎处理了输入事件、场景树数据变化"（日志层面）

## 验收标准
- ① 点击/拖拽注入后引擎场景树/选择状态发生变化（诊断日志或后续 T 用例）
- ② 节点创建/删除/变换操作无崩溃
- ③ gizmo 拖拽（左键拖动）产生 mouse motion 事件链（T04 延伸）

## 任务
- [ ] T-SE-1 场景编辑操作日志级验证用例：点击节点→选中状态变化（引擎日志）
- [ ] T-SE-2 变换 gizmo 拖拽验证：mouse motion + button_mask 链路（代码路径已实现，未实测）

## 开放项
- 画面级反馈验证受限（模拟器限制），视觉正确性无验证环境。