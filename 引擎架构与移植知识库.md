# Godot 引擎源码架构知识库

## 一、项目概况

| 属性 | 值 |
|---|---|
| 项目名 | `godot-hos` (Godot Engine - HarmonyOS Port) |
| 上游 | [godotengine/godot](https://github.com/godotengine/godot) |
| Fork | [Ralph-sa/godot](https://github.com/Ralph-sa/godot) (harmonyos 分支) |
| 版本 | 4.8.0 (harmonyos) |
| 许可证 | MIT |
| 语言 | C++17 (引擎核心) / Python (构建系统) / C (NAPI 桥接) |
| 构建系统 | SCons (引擎) / CMake (NAPI 桥接) / hvigor (HAP 打包) |

---

## 二、引擎顶层目录结构

```
godot_src/
├── core/          # 引擎核心：数学、数据结构、OS抽象、输入、资源管理、Variant等
├── drivers/       # 平台驱动：渲染(Vulkan/GLES)、音频、窗口管理等底层驱动
├── editor/        # Godot 编辑器（仅在 target=editor 时编译）
├── main/          # 主入口点和性能监控 (Main::setup / Main::start / Main::cleanup)
├── modules/       # 可插拔模块：GDScript、GDNative、物理引擎(bullet/jolt)、导航等
├── platform/      # 平台抽象层（每平台一个子目录，实现 OS/DisplayServer/Input/Audio 等接口）
├── scene/         # 场景系统：节点树、GUI、动画、资源、渲染管线管理
├── servers/       # 服务层：RenderServer、PhysicsServer、AudioServer 等
├── thirdparty/    # 第三方库：freetype、vulkan、png、enet、basis_universal 等
├── tests/         # 单元测试
├── misc/          # 杂项资源（logo、分发模板等）
├── doc/           # 文档相关
│
├── SConstruct     # SCons 构建入口(~1263行)：扫描平台+模块、设置编译选项、编排构建顺序
├── methods.py     # 通用 SCons 辅助方法
├── platform_methods.py  # 平台工具函数（架构检测、版本号、Apple/MoltenVK 等）
├── version.py     # 版本信息定义
└── pyproject.toml # Python 项目元数据
```

### 构建顺序（SConstruct 编排）

```
core → servers → scene → editor(条件) → drivers → platform → modules → tests(条件) → main
```

---

## 三、核心子系统详解

### 3.1 core/ — 引擎核心

不依赖任何平台的头文件库，提供全引擎共用的基础设施：

| 子系统 | 关键类 | 职责 |
|---|---|---|
| 数学库 | `Vector2/3/4`, `AABB`, `Basis`, `Transform2D/3D`, `Quaternion`, `Projection` | 图形学数学运算 |
| 数据结构 | `Vector<T>`, `HashMap<K,V>`, `HashSet<T>`, `List<T>`, `RBMap<K,V>` | 高性能容器 |
| 类型系统 | `Variant`, `Object`, `ClassDB`, `MethodBind` | 动态类型绑定、反射、序列化 |
| OS 抽象 | `OS`, `OS_Unix` | 平台接口定义（基类定义虚函数，由各平台实现） |
| 输入 | `Input`, `InputMap`, `InputEvent*` | 输入事件抽象与映射 |
| 字符串 | `String`, `StringName`, `CharString` | COW 字符串 + 驻留优化 |
| 资源管理 | `Resource`, `ResourceLoader`, `ResourceFormatLoader/Saver` | 资源加载/保存管线 |
| 内存 | `Memory`, `SafeRefCount`, `memnew/memdelete` | 自定义内存分配器 |
| IO | `FileAccess`, `DirAccess`, `NetSocket` | 跨平台文件/网络 IO 抽象 |
| 线程 | `Thread`, `Mutex`, `Semaphore`, `WorkerThreadPool`, `SpinLock` | 并发原语 |
| 脚本 | `Script`, `ScriptInstance`, `ScriptLanguage` | 多语言脚本框架 |
| 引擎状态 | `Engine`, `ProjectSettings`, `MainLoop` | 全局引擎配置与主循环 |

### 3.2 servers/ — 服务层

对外提供线程安全的服务端 API，内部与驱动层通信：

| 服务 | 关键类 | 职责 |
|---|---|---|
| 渲染 | `RenderingServer`, `RenderSceneData`, `RenderingDevice` | 场景树→绘制指令转换 |
| 物理 | `PhysicsServer2D/3D`, `PhysicsDirectSpaceState2D/3D` | 碰撞检测、刚体模拟 |
| 音频 | `AudioServer`, `AudioBus`, `AudioStream` | 混音、总线路由 |
| 显示 | `DisplayServer` | 窗口管理、剪贴板、光标、屏幕信息（平台实现接口） |
| 文本 | `TextServer`, `TextServerFallback` | 字形光栅化、文本排版 |
| 导航 | `NavigationServer2D/3D` | 寻路网格管理 |
| XR | `XRServer`, `XRInterface` | VR/AR 接口 |

### 3.3 scene/ — 场景系统

Godot 的核心游戏对象模型：

| 子系统 | 关键类 | 职责 |
|---|---|---|
| 节点树 | `Node`, `SceneTree`, `Viewport`, `Window` | 场景树管理、时间步进 |
| GUI | `Control`, `CanvasItem`, `Theme`, `Font` | 2D UI 系统 |
| 2D 节点 | `Node2D`, `Sprite2D`, `TileMap`, `Camera2D` | 2D 游戏对象 |
| 3D 节点 | `Node3D`, `MeshInstance3D`, `Camera3D`, `Skeleton3D` | 3D 游戏对象 |
| 资源 | `PackedScene`, `Mesh`, `Material`, `Shader`, `Texture2D` | 可序列化资源 |
| 动画 | `AnimationPlayer`, `AnimationTree`, `Tween` | 动画系统 |
| 物理节点 | `CollisionShape2D/3D`, `RigidBody2D/3D`, `CharacterBody2D/3D` | 物理组件 |
| 音频节点 | `AudioStreamPlayer2D/3D` | 音频播放器 |
| 环境 | `Environment`, `WorldEnvironment`, `Sky`, `FogVolume` | 场景级渲染设置 |

### 3.4 drivers/ — 驱动层

实现平台相关的底层接口，被 servers 层调用：

| 驱动类型 | 位置 | 说明 |
|---|---|---|
| Vulkan | `drivers/vulkan/` | Vulkan 渲染上下文、管线缓存、SwapChain |
| OpenGL 3 | `drivers/gles3/` | GLES 3.0 渲染后端 |
| D3D12 | `drivers/d3d12/` | DirectX 12 渲染后端 |
| Metal | `drivers/metal/` | Apple Metal 渲染后端 |
| 音频 | 内核 | PulseAudio、WASAPI、CoreAudio、OHAudio 等 |
| Unix OS | `drivers/unix/` | POSIX 系统调用封装（被 HarmonyOS/Linux/BSD 继承） |

### 3.5 platform/ — 平台抽象层

每个目标平台一个子目录，实现 Godot 定义的平台接口：

| 平台 | 目录 | 说明 |
|---|---|---|
| Windows | `platform/windows/` | Win32 / DirectWrite / WASAPI / COM |
| Linux/BSD | `platform/linuxbsd/` | X11 / Wayland / PulseAudio / DBus |
| macOS | `platform/macos/` | Cocoa / Metal / CoreAudio |
| iOS | `platform/ios/` | UIKit / Metal / CoreAudio |
| visionOS | `platform/visionos/` | Vision Pro 平台 |
| Android | `platform/android/` | JNI / SurfaceView / AAudio / OpenSL ES |
| Web | `platform/web/` | Emscripten / WebGL / WebAudio |
| **HarmonyOS** | `platform/harmonyos/` | **本项目移植目标** |

每个平台目录必须包含：

| 文件 | 作用 |
|---|---|
| `detect.py` | 平台检测：工具链配置、架构支持、编译标志、链接库、预处理器宏 |
| `SCsub` | 平台源文件构建列表 |
| `os_<platform>.cpp/.h` | OS 抽象层实现 |
| `display_server_<platform>.cpp/.h` | DisplayServer 窗口管理实现 |
| 可选 | 音频驱动、输入处理、崩溃处理、平台特定功能 |

---

## 四、Godot 跨平台架构设计

### 4.1 分层架构

```
┌─────────────────────────────────────────────────┐
│  GDScript / C# / GDextension 脚本层             │
├─────────────────────────────────────────────────┤
│  scene/ — 场景系统 (Node/SceneTree/Viewport)     │
├─────────────────────────────────────────────────┤
│  servers/ — 服务层 (Rendering/Physics/Audio)     │
├─────────────────────────────────────────────────┤
│  core/ — 引擎核心 (Variant/Object/Math)          │
├─────────────────────────────────────────────────┤
│  drivers/ — 驱动层                               │
│  ├─ Rendering (Vulkan/GLES/D3D12/Metal)         │
│  ├─ Audio (PulseAudio/WASAPI/OHAudio)           │
│  └─ Unix OS (POSIX 抽象)                        │
├─────────────────────────────────────────────────┤
│  platform/ — 平台抽象层                          │
│  ├─ windows/  ├─ linuxbsd/  ├─ macos/          │
│  ├─ ios/      ├─ android/   ├─ web/            │
│  └─ harmonyos/ ◄── 移植目标                      │
├─────────────────────────────────────────────────┤
│  OS 内核 (NT / Linux / Darwin / OHOS)            │
└─────────────────────────────────────────────────┘
```

### 4.2 平台抽象接口体系

每个新平台必须实现的接口类：

| 接口类 | 定义位置 | 核心职责 |
|---|---|---|
| `OS` / `OS_Unix` | `core/os/os.h` / `drivers/unix/os_unix.h` | 文件路径、进程管理、动态库加载、熵源、内存信息 |
| `DisplayServer` | `servers/display/display_server.h` | 窗口创建/管理、剪贴板、屏幕信息、光标、IME、事件分发、SwapChain |
| `RenderingContextDriver` | `drivers/vulkan/rendering_context_driver_vulkan.h` | Vulkan Instance/Device 创建、Surface、扩展查询 |
| `AudioDriver` | `servers/audio_server.h` | 音频设备枚举、音频流播放、混音回调 |
| `Input` 处理 | 平台特定 | 触摸/键盘/鼠标/手柄事件从系统事件→Godot InputEvent 转换 |
| `FileAccess` | `core/io/file_access.h` | 文件读写（可继承基类实现） |
| `DirAccess` | `core/io/dir_access.h` | 目录遍历、文件系统操作 |

### 4.3 条件编译机制

Godot 通过 SCons 构建系统注入平台宏：

```cpp
// 平台标识宏（由 detect.py 注入）
HARMONYOS_ENABLED   // HarmonyOS 平台
WINDOWS_ENABLED     // Windows
LINUXBSD_ENABLED    // Linux/BSD
MACOS_ENABLED       // macOS
IOS_ENABLED         // iOS
ANDROID_ENABLED     // Android
WEB_ENABLED         // Web/Emscripten

// 功能宏
UNIX_ENABLED        // 类 Unix 系统（POSIX API 可用）
VULKAN_ENABLED      // Vulkan 渲染后端启用
GLES3_ENABLED       // OpenGL ES 3 后端启用
RD_ENABLED          // RenderingDevice 启用
TOOLS_ENABLED       // 编辑器模式（仅 editor 构建目标）
```

---

## 五、跨平台引擎移植实践指南

> 以 Godot → HarmonyOS 移植为案例

### 5.1 核心原则

| 原则 | 说明 |
|---|---|
| **不修改引擎核心** | `core/`、`scene/`、`servers/`、`modules/` 等代码一律不动 |
| **接口实现优先** | 在 `platform/<target>/` 中实现引擎定义的纯虚接口 |
| **条件编译最小化** | 仅在万不得已时用 `#ifdef HARMONYOS_ENABLED` 隔离 |
| **基准参照** | 选成熟平台（如 Windows）作为架构参照 |
| **构建系统接入** | 通过 `detect.py` + `SCsub` 接入 SCons |

### 5.2 移植流程（五阶段法）

#### 阶段一：脚手架搭建

1. 创建 `platform/<target>/` 目录
2. 编写 `detect.py`（工具链检测、架构声明、编译标志、预处理器宏）
3. 编写 `SCsub`（源文件列表）
4. 创建 `platform_config.h`（平台配置头文件）

**detect.py 关键要素：**

```python
def get_name():          # 平台名称
def can_build():         # 能否在当前宿主编译（检测 SDK）
def configure(env):      # 注入编译标志、链接库、宏定义
def get_flags():         # 默认架构、默认构建目标
def get_opts():          # 自定义构建选项
```

**必需注入的宏：**

```python
env.Append(CPPDEFINES=[
    "HARMONYOS_ENABLED",   # 条件编译开关
    "VULKAN_ENABLED",      # 渲染后端
    "UNIX_ENABLED",        # POSIX API 可用
])
```

#### 阶段二：OS 抽象层

实现 `OS_<target>` 类（通常继承 `OS_Unix`，因 HarmonyOS 使用 musl libc 提供 POSIX API）：

```cpp
class OS_HarmonyOS : public OS_Unix {
    // 必须重写的方法：
    String get_name() const override;
    String get_user_data_dir(...) const override;  // 沙箱路径映射
    String get_cache_path() const override;
    String get_config_path() const override;
    String get_data_path() const override;
    void initialize() override;       // 平台初始化（日志、线程、信号）
    void finalize() override;         // 清理资源
    void set_main_loop(...) override; // 设置主循环
    Error open_dynamic_library(...) override; // dlopen 封装
    String get_executable_path() const override;
    Error execute(...) override;       // 进程创建
    Error get_entropy(...) override;   // 随机数熵源
    bool _check_internal_feature_support(...) override;
};
```

**沙箱路径映射（HarmonyOS 特有）：**

```
/data/storage/el2/base/haps/entry/files/ → user data
/data/storage/el2/base/haps/entry/cache/ → cache
```

#### 阶段三：DisplayServer

实现 `DisplayServer<target>` 类（继承 `DisplayServer`），这是移植中工作量最大的接口：

```cpp
class DisplayServerHarmonyOS : public DisplayServer {
    // 必须实现的接口模块：

    // -- 窗口管理 (15+ 方法) --
    window_set_title, window_get_size, window_set_size, window_get_position,
    window_set_mode, window_get_mode, window_set_flag, window_get_flag,
    window_is_focused, window_can_draw, window_move_to_foreground, ...

    // -- 屏幕信息 (8+ 方法) --
    get_screen_count, screen_get_size, screen_get_dpi, screen_get_scale,
    screen_get_refresh_rate, ...

    // -- 事件回调注册 --
    window_set_window_event_callback   // 窗口关闭/焦点/大小变化
    window_set_input_event_callback    // 键鼠/触摸
    window_set_input_text_callback     // 文本输入（IME）
    window_set_rect_changed_callback   // 窗口移动/大小
    window_set_drop_files_callback     // 拖放文件

    // -- 输入 --
    process_events()           // 主循环事件泵，每次帧调用
    cursor_set_shape           // 光标形状

    // -- 帧同步 --
    swap_buffers()             // SwapChain present

    // -- 注册工厂方法（静态） --
    static DisplayServer *create_func(...);
    static Vector<String> get_rendering_drivers_func();
};
```

**HarmonyOS 实现要点：**

- 用 `XComponent` / `OHNativeWindow` 作为渲染表面
- 通过 NAPI 桥接从 ArkTS 获取 XComponent 实例
- 实现 `OnSurfaceCreated_CB` / `OnSurfaceChanged_CB` / `OnSurfaceDestroyed_CB` 回调
- 实现 `DispatchTouchEvent_CB` 处理触摸输入

#### 阶段四：渲染上下文

实现 `RenderingContextDriverVulkan<target>`（继承 `RenderingContextDriverVulkan`）：

```cpp
class RenderingContextDriverVulkanHarmonyOS : public RenderingContextDriverVulkan {
    const char *_get_platform_surface_extension() const override;
    SurfaceID surface_create(const void *p_platform_data) override;
    bool _use_validation_layers() const override;
};
```

**关键点：**
- 返回平台特定的 Vulkan Surface 扩展名（如 `VK_OHOS_surface`）
- `surface_create` 接收 `OHNativeWindow*` 创建 `VkSurfaceKHR`

#### 阶段五：桥接层

对于不能直接使用 POSIX API 的平台，需要实现原生→引擎的桥接：

```
ArkTS (前端) ←→ NAPI Bridge (entry/src/main/cpp/) ←→ libgodot.so
```

**NAPI 桥接核心功能：**

```cpp
// 生命周期管理
harmonyos_godot_init()          // 初始化引擎（Main::setup）
harmonyos_godot_cleanup()       // 清理引擎（Main::cleanup）
harmonyos_godot_on_pause()      // 应用暂停（释放 Vulkan 上下文）
harmonyos_godot_on_resume()     // 应用恢复（重建 Vulkan 上下文）

// Surface 管理
harmonyos_godot_surface_created(surface_id)   // XComponent surface 就绪
harmonyos_godot_surface_destroy()             // Surface 销毁

// 输入事件转发
harmonyos_godot_key_event(key_code, type, text)
harmonyos_godot_mouse_event(button, action, x, y, offsetX, offsetY)
harmonyos_godot_touch_event(touch_id, action, x, y)
harmonyos_godot_input_text(text)
```

**桥接设计要点：**
- 使用 `dlopen` / `dlsym` 动态加载 `libgodot.so` 并解析导出符号
- 导出函数使用 `__attribute__((visibility("default")))` 确保符号可见
- 使用 `std::atomic<bool>` 管理引擎状态转换

### 5.3 移植检查清单

```
□ 新平台目录已创建
□ detect.py 已实现（工具链、架构、编译标志、宏、链接库）
□ SCsub 已编写
□ OS 抽象层已实现（文件路径、进程、动态库、熵源）
□ DisplayServer 已实现（窗口、屏幕、事件、IME、SwapChain）
□ 渲染上下文驱动已实现（Vulkan Surface 扩展）
□ 音频驱动已实现（可选，可先返回 noop）
□ 输入处理已实现（触摸/键盘/鼠标/手柄）
□ 崩溃处理器已实现
□ NAPI/原生桥接层已实现（若非标准 POSIX 平台）
□ 条件编译宏正确隔离
□ 不修改引擎核心代码
```

### 5.4 常见陷阱

| 陷阱 | 说明 | 解法 |
|---|---|---|
| **符号可见性** | `-fvisibility=hidden` 后 dlopen 找不到符号 | 用 `__attribute__((visibility("default")))` 标记导出函数 |
| **沙箱路径** | 移动系统的文件系统在沙箱内，不是标准 Unix 路径 | 必须映射用户/缓存/数据目录到系统提供的沙箱路径 |
| **Surface 生命周期** | 移动/嵌入式平台 Surface 随时创建/销毁 | 实现原子状态机，正确处理 pause/resume 重建 Vulkan 上下文 |
| **事件泵** | 非标准事件循环需要桥接 | 在 `process_events()` 中实现事件转发 |
| **toolchain triple** | 交叉编译工具链命名不统一 | 使用 `--target=aarch64-linux-ohos` 显式指定 |
| **sysroot** | 缺少标准库头文件/库 | 通过 `--sysroot=` 指向 SDK sysroot |

---

## 六、HarmonyOS 移植详情

### 6.1 移植文件清单

```
platform/harmonyos/
├── detect.py                                    # 编译配置（OHOS LLVM、arm64-only、Vulkan/GLES）
├── SCsub                                        # 源文件构建列表
├── platform_config.h                            # 平台配置
├── platform_gl.h / platform_egl.h               # OpenGL/EGL 包装
├── vulkan_ohos_surface.h                        # VK_OHOS_surface 扩展定义
│
├── crash_handler_harmonyos.cpp/.h               # 崩溃信号处理器
├── dir_access_harmonyos.cpp/.h                  # 目录操作
├── file_access_harmonyos.cpp/.h                 # 文件访问
├── display_server_harmonyos.cpp/.h              # DisplayServer 实现（窗口/事件/IME/SwapChain）
├── harmonyos_audio.cpp/.h                       # OHAudio 音频驱动
├── harmonyos_input.cpp/.h                       # 输入处理（触摸/键盘/鼠标/手柄）
├── harmonyos_main.cpp/.h                        # 引擎生命周期入口（init/surface/cleanup/pause/resume）
├── harmonyos_native_window.cpp/.h               # XComponent NativeWindow 管理
├── joypad_harmonyos.cpp/.h                      # 手柄支持
├── os_harmonyos.cpp/.h                          # OS 抽象层（继承 OS_Unix）
├── tts_harmonyos.cpp/.h                         # 文字转语音
├── rendering_context_driver_vulkan_harmonyos.cpp/.h  # Vulkan 渲染上下文
│
└── export/                                      # 导出子系统
    ├── export.cpp/.h                            # HAP 导出
    ├── export_plugin.cpp/.h                     # 导出插件
    ├── logo.svg                                 # 平台 Logo SVG
    └── logo_svg.gen.h                           # Logo 生成头文件
```

### 6.2 NAPI 桥接层

```
entry/src/main/cpp/
├── CMakeLists.txt           # CMake 构建
├── godot_napi_bridge.cpp    # NAPI 桥接主文件（dlopen 加载 libgodot.so，导出 12 个 NAPI 函数）
└── types/
    └── libgodot_napi/
        ├── Index.d.ts       # TypeScript 类型声明
        └── oh-package.json5 # OHPM 包配置
```

**NAPI 导出的 12 个函数：**

| 函数 | 作用 |
|---|---|
| `init` | 初始化 Godot 引擎（调用 `harmonyos_godot_init`） |
| `cleanup` | 清理引擎（调用 `harmonyos_godot_cleanup`） |
| `onSurfaceCreated` | 通知 Surface 创建（传入 surfaceId） |
| `onSurfaceDestroy` | 通知 Surface 销毁 |
| `sendKeyEvent` | 转发键盘事件 |
| `sendMouseEvent` | 转发鼠标事件 |
| `sendTouchEvent` | 转发触摸事件 |
| `sendInputText` | 转发文本输入 |
| `onPause` | 应用暂停（释放 Vulkan） |
| `onResume` | 应用恢复（重建 Vulkan） |
| `onBackPress` | 返回键处理 |
| `registerTitleCallback` | 注册窗口标题回调（从 C++ 通知 ArkTS 更新标题） |

### 6.3 工具链与技术栈

| 层次 | 技术 |
|---|---|
| 宿主编译环境 | Windows x86_64 |
| 目标平台 | HarmonyOS (arm64, Kirin X90) |
| 编译器 | OHOS LLVM clang.exe / clang++.exe |
| 目标三元组 | `aarch64-linux-ohos` |
| C 运行时 | musl libc (POSIX 兼容) |
| 图形 API (主) | Vulkan (VK_OHOS_surface 扩展) |
| 图形 API (备) | OpenGL ES 3.0 |
| 窗口系统 | XComponent / OHNativeWindow |
| 音频 | OHAudio |
| 日志 | HiLog |
| UI 框架 | ArkUI (用于宿主应用壳) |
| 桥接技术 | NAPI (Node-API 风格) |
| 动态加载 | dlopen / dlsym |
| 打包格式 | HAP (HarmonyOS Ability Package) |

---

## 七、实用资源

| 资源 | 链接 |
|---|---|
| Godot 引擎架构文档 | https://docs.godotengine.org/en/latest/engine_details/architecture/index.html |
| Godot SCons 构建系统 | `godot_src/SConstruct` |
| 本移植上游仓库 | https://github.com/Ralph-sa/godot |
| Godot 上游主仓库 | https://github.com/godotengine/godot |
| HarmonyOS 原生 API 文档 | https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/native-apis |
